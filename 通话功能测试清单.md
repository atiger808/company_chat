# 通话功能测试清单

## 📋 测试前准备

1. **清除浏览器缓存**
   ```
   Ctrl + F5（强制刷新）
   ```

2. **打开浏览器开发者工具**
   - 按F12
   - 切换到Console标签
   - 保持控制台开启状态

3. **重启Django服务器**
   ```bash
   # 停止当前服务器（Ctrl+C）
   python manage.py runserver
   ```

---

## ✅ 测试场景1：正常通话（时长正确显示）

### 操作步骤
1. 用户A登录系统
2. 用户B登录系统（不同浏览器或无痕模式）
3. 用户A进入与用户B的聊天室
4. 用户A点击"语音通话"按钮
5. 用户B在弹窗中点击"接听"
6. 等待5-10秒
7. 用户A或用户B点击"挂断"按钮

### 预期结果
- ✅ 前端日志显示：
  ```
  ⏱️ 通话时长: 7 秒 (callStartTime: 1715832361772)
  📤 准备发送 call_end 信令: {"type":"call_end","room_id":114,"from_user_id":9,"duration":7,"media_type":"audio"}
  ✅ 已发送 call_end 信令，时长: 7 秒
  ```

- ✅ 后端日志显示：
  ```
  📤 转发 call_end 信令: from=9, room=114, duration=7s, media_type=audio, reason=ended
  ✅ 通话记录消息已创建: message_id=9108, room=114, status=completed
  ```

- ✅ 聊天室显示：
  ```
  语音通话 7秒
  ```
  （不是"语音通话 0秒"）

- ✅ 双方都能看到这条记录

### 如果失败
- 检查前端日志是否有 `⚠️ callStartTime 为空，通话时长将为0`
- 检查后端日志中 `duration=Xs` 的X是否为0
- 查看完整的日志输出，找出问题所在

---

## ✅ 测试场景2：拒绝通话

### 操作步骤
1. 用户A发起语音通话给用户B
2. 用户B在来电弹窗中点击"拒绝"按钮

### 预期结果
- ✅ 用户B前端日志显示：
  ```
  ❌ 用户拒绝来电
  📤 已发送 call_reject 信令
  ✅ 拒绝来电，资源已清理
  ```

- ✅ 后端日志显示：
  ```
  📤 转发 call_reject 信令: from=5 to=9, room=114, media_type=audio
  ✅ 通话记录消息已创建: message_id=9109, room=114, status=rejected
  ```

- ✅ 用户A前端日志显示：
  ```
  ❌ 通话被拒绝
  ```

- ✅ 聊天室显示：
  ```
  已拒绝语音通话
  ```

- ✅ 双方都能看到这条记录

### 如果失败
- 检查前端是否发送了`call_reject`信令
- 检查后端是否收到了`call_reject`信令
- 检查后端是否创建了通话记录

---

## ✅ 测试场景3：未接听超时

### 操作步骤
1. 用户A发起语音通话给用户B
2. 用户B不接听，等待60秒

### 预期结果
- ✅ 60秒后用户B前端日志显示：
  ```
  ⏱️ 来电超时60秒，自动挂断
  ⏱️ 通话未接听，超时自动挂断
  📤 已发送 call_missed 信令
  ✅ 未接听通话，资源已清理
  ```

- ✅ 用户B看到提示：
  ```
  对方未接听
  ```

- ✅ 后端日志显示：
  ```
  📤 转发 call_missed 信令: from=5 to=9, room=114, media_type=audio
  ✅ 通话记录消息已创建: message_id=9110, room=114, status=missed
  ```

- ✅ 用户A前端日志显示：
  ```
  ⏱️ 通话未接听
  ```

- ✅ 用户A看到提示：
  ```
  对方未接听
  ```

- ✅ 聊天室显示：
  ```
  未接听语音通话
  ```

- ✅ 双方都能看到这条记录

### 如果失败
- 检查定时器是否正确启动（60秒）
- 检查是否调用了`handleCallMissed`方法
- 检查是否发送了`call_missed`信令

---

## ✅ 测试场景4：视频通话

### 操作步骤
1. 用户A发起视频通话给用户B
2. 用户B接听
3. 等待5-10秒后挂断

### 预期结果
- ✅ 聊天室显示：
  ```
  视频通话 7秒
  ```
  （注意是"视频通话"而不是"语音通话"）

### 如果失败
- 检查前端发送的`media_type`是否为`video`
- 检查后端接收到的`media_type`参数

---

## 🐛 常见问题排查

### 问题1：通话时长还是0秒
**可能原因**：
- `callStartTime`没有正确设置
- WebSocket连接在发送信令前就断开了

**排查步骤**：
1. 检查前端日志是否有 `⏱️ 呼叫开始时间:` 或 `⏱️ 通话开始时间:`
2. 检查前端日志是否有 `⚠️ callStartTime 为空，通话时长将为0`
3. 检查后端日志中 `duration=Xs` 的值

### 问题2：拒绝通话没有记录
**可能原因**：
- 前端没有发送`call_reject`信令
- 后端没有调用`create_call_record_message`

**排查步骤**：
1. 检查前端日志是否有 `📤 已发送 call_reject 信令`
2. 检查后端日志是否有 `📤 转发 call_reject 信令`
3. 检查后端日志是否有 `✅ 通话记录消息已创建`

### 问题3：超时没有自动挂断
**可能原因**：
- 定时器没有正确启动
- `callState`不是`ringing`状态

**排查步骤**：
1. 检查前端日志是否有 `⏱️ 来电超时60秒，自动挂断`
2. 检查`callState`的值是否为`ringing`
3. 检查是否调用了`handleCallMissed`方法

---

## 📊 测试结果记录

| 测试场景 | 测试结果 | 备注 |
|---------|---------|------|
| 正常通话（时长显示） | ☐ 通过 ☐ 失败 |  |
| 拒绝通话（记录显示） | ☐ 通过 ☐ 失败 |  |
| 未接听超时（记录显示） | ☐ 通过 ☐ 失败 |  |
| 视频通话（类型显示） | ☐ 通过 ☐ 失败 |  |

---

## 🎯 成功标准

所有测试场景都通过后，应该满足以下条件：

1. ✅ 正常通话后，聊天室显示正确的通话时长（不是0秒）
2. ✅ 拒绝通话后，聊天室显示"已拒绝语音通话"
3. ✅ 超时未接听后，聊天室显示"未接听语音通话"
4. ✅ 视频通话后，聊天室显示"视频通话 X秒"
5. ✅ 所有通话记录对双方都可见
6. ✅ 前后端日志清晰，便于调试

---

## 📞 需要帮助？

如果测试过程中遇到问题：
1. 复制完整的前端日志（Console输出）
2. 复制完整的后端日志（Django日志）
3. 说明具体的测试步骤和预期结果
4. 提供截图（如果有UI问题）
