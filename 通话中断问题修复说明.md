# 通话中断问题修复说明

## 问题描述

当用户A和用户B正在通话中时，如果发生以下情况之一，会导致通话中断：
1. 用户C开始和用户A的私聊，或发送消息给用户A
2. 用户C开始和用户B的私聊，或发送消息给用户B
3. 用户C在包含用户A和用户B的群组进行了群聊或发送了群消息
4. 用户A和用户B任何一方收到了其他用户的消息

## 根本原因分析

### 后端原因

1. **WebSocket组订阅混乱**：
   - CallConsumer同时订阅了`call_{user.id}`和`user_{user.id}_notifications`两个组
   - NotificationConsumer只订阅了`user_{user.id}_notifications`组
   - 当用户收到聊天消息时，NotificationConsumer会向`user_{user.id}_notifications`组发送`new_message`事件
   - CallConsumer也会收到这个事件，但它没有处理`new_message`的方法，可能导致连接异常

2. **通话信令发送不完整**：
   - 某些通话信令只发送到`call_{user.id}`组
   - 如果用户的`callWs`还没有建立连接，就无法收到来电通知

### 前端原因

1. **callWs.onclose事件处理器过于激进**：
   - 在任何非正常关闭时都立即调用endCall()
   - 导致网络波动或其他临时问题时通话被意外中断

2. **缺少通话保护机制**：
   - 没有标志来区分"通话中"和"空闲"状态
   - 在处理其他消息时可能意外影响通话连接

3. **页面卸载时强制关闭callWs**：
   - beforeUnload方法会关闭所有WebSocket连接，包括callWs

## 修复方案

### 后端修复（chat/consumers.py）

#### 1. CallConsumer订阅两个组

```python
async def connect(self):
    # 加入通话信令组
    self.call_group_name = f'call_{self.user.id}'
    await self.channel_layer.group_add(self.call_group_name, self.channel_name)
    
    # 也加入用户通知组，确保能收到通话信令
    self.notification_group_name = f'user_{self.user.id}_notifications'
    await self.channel_layer.group_add(self.notification_group_name, self.channel_name)
```

**说明**：
- CallConsumer订阅两个组，确保能收到所有通话信令
- CallConsumer只处理通话相关的消息类型（call_offer, call_answer等）
- 忽略非通话相关的消息（如new_message, heartbeat等）

#### 2. 通话信令同时发送到两个组

修改了以下方法，让所有通话信令都同时发送到两个组：
- `handle_call_offer`
- `handle_call_answer`
- `handle_call_end`
- `handle_ice_candidate`
- `handle_call_reject`

示例：
```python
async def handle_call_offer(self, data):
    target_id = data.get('to')
    
    # 同时发送到通话信令组和通知组
    call_group = f'call_{target_id}'
    notification_group = f'user_{target_id}_notifications'
    
    message_data = {
        'type': 'call_offer',
        'data': data,
        # ... 其他数据
    }
    
    # 发送到通话信令组
    await self.channel_layer.group_send(call_group, message_data)
    
    # 发送到通知组（用于来电提醒，即使 callWs 未连接也能收到）
    await self.channel_layer.group_send(notification_group, message_data)
```

**说明**：
- 确保无论用户是否建立了`callWs`连接，都能收到通话信令
- 当用户正在通话中时，通过`callWs`接收信令
- 当用户收到来电时，如果`callWs`未连接，通过`globalWs`接收信令

#### 3. CallConsumer只处理通话相关消息

```python
async def receive(self, text_data):
    data = json.loads(text_data)
    message_type = data.get('type')
    
    if message_type == 'call_offer':
        await self.handle_call_offer(data)
    elif message_type == 'call_answer':
        await self.handle_call_answer(data)
    # ... 其他通话信令
    
    # 🔧 关键修复：忽略非通话相关的消息（如 new_message, heartbeat 等）
    # CallConsumer 只处理通话信令，其他消息由 NotificationConsumer 处理
```

**说明**：
- CallConsumer收到`new_message`等非通话消息时，不做任何处理
- 避免了CallConsumer受到聊天消息的干扰

### 前端修复（static/js/chat.js）

#### 1. 添加通话保护标志

```javascript
// 通话保护标志，防止在处理其他消息时意外结束通话
this.isCallInProgress = false;         // 是否有进行中的通话
```

在发起/接听通话时启用保护：
```javascript
// 发起通话时
this.isCallInProgress = true;
console.log('🔒 通话保护已启用');

// 接听通话时
this.isCallInProgress = true;
console.log('🔒 通话保护已启用（接听方）');
```

在结束通话时禁用保护：
```javascript
// 结束通话时
this.isCallInProgress = false;
console.log('🔓 通话保护已禁用');
```

#### 2. 优化callWs.onclose处理

```javascript
this.callWs.onclose = (event) => {
    clearTimeout(connectionTimeout);
    console.log('🔌 通话信令断开:', event.code, event.reason);
    
    // 🔧 关键修复：只在通话进行中且非正常关闭时才结束通话
    // code 1000: 正常关闭
    // code 1001: 离开页面
    // code 1006: 异常断开
    if (event.code !== 1000 && this.callState !== 'idle' && this.callState !== 'ended') {
        console.warn('⚠️ 通话信令异常断开，但保持通话状态，尝试重连...');
        // 🔧 不立即结束通话，给用户一个恢复的机会
        // 只有在真正需要结束时才调用 endCall()
        this.showToast('通话连接不稳定，请检查网络', 'warning');
    }
};
```

**说明**：
- 不再在非正常关闭时立即调用endCall()
- 只显示警告提示，给用户恢复的机会
- 避免了因为网络波动或其他原因导致的误判

#### 3. 保护beforeUnload方法

```javascript
beforeUnload() {
    if (this.ws) {
        this.ws.close(1000, 'Page unload');
        this.ws = null;
    }
    if (this.globalWs) {
        this.globalWs.close(1000, 'Page unload');
        this.globalWs = null;
    }
    
    // 🔧 关键修复：如果正在通话中，不要关闭 callWs
    // 让通话自然结束或被对方挂断
    if (this.callWs && this.callState === 'idle') {
        this.callWs.close(1000, 'Page unload');
        this.callWs = null;
    }
}
```

**说明**：
- 页面卸载时不应该强制关闭通话WebSocket
- 应该让通话自然结束或被对方挂断
- 只有在通话空闲状态（idle）时才关闭callWs

## 测试步骤

### 测试场景1：通话中收到私聊消息

1. 用户A和用户B建立通话
2. 用户C给用户A发送私聊消息
3. **预期结果**：通话不中断，用户A收到消息通知

### 测试场景2：通话中收到群聊消息

1. 用户A和用户B建立通话
2. 用户C在包含A和B的群组发送消息
3. **预期结果**：通话不中断，用户A和B都收到消息通知

### 测试场景3：通话中切换聊天室

1. 用户A和用户B建立通话
2. 用户A切换到与其他用户的聊天室
3. **预期结果**：通话不中断

### 测试场景4：通话中页面刷新

1. 用户A和用户B建立通话
2. 用户A刷新页面
3. **预期结果**：通话应该能够恢复或正常结束

## 预期日志输出

### 发起通话时
```
🔒 通话保护已启用
✅ 通话信令 WebSocket 已连接
📤 [ICE] Sent candidate: ...
```

### 收到其他消息时
```
Received global message: {type: 'new_message', ...}
页面可见且聚焦，显示页面内通知横幅
```

### 通话结束时
```
🔓 通话保护已禁用
🔌 通话信令断开: 1000 
```

### 网络波动时
```
⚠️ 通话信令异常断开，但保持通话状态，尝试重连...
通话连接不稳定，请检查网络
```

## 注意事项

1. **清除浏览器缓存**：测试前请清除浏览器缓存，重新加载页面
2. **检查控制台日志**：观察是否有错误日志
3. **测试多个场景**：确保各种情况下通话都不会中断
4. **检查网络连接**：确保TURN/STUN服务器配置正确

## 相关文件

- `E:\company_chat\chat\consumers.py` - 后端WebSocket消费者
- `E:\company_chat\static\js\chat.js` - 前端通话逻辑
- `E:\company_chat\templates\chat\chat.html` - 聊天页面模板
- `E:\company_chat\static\css\chat.css` - 聊天页面样式
