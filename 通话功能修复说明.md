# 通话功能修复说明

## 修复内容

### 1. 移动端按钮显示问题 ✅

**问题描述：**
- 接通电话后，移动端没有正确显示控制按钮（静音、摄像头开关、前后摄像头切换、免提）

**根本原因：**
- HTML模板中按钮ID是`cameraBtn`，但JavaScript代码中查找的是`btnVideoToggle`
- 缺少免提按钮

**修复方案：**
1. **修改 JavaScript 代码** (`chat.js` 第11820-11847行)
   - 将`document.getElementById('btnVideoToggle')`改为`document.getElementById('cameraBtn')`
   - 添加对`speakerBtn`（免提按钮）的显示/隐藏控制

2. **修改 HTML 模板** (`chat.html` 第747-765行)
   - 添加免提按钮：
     ```html
     <button class="call-btn" id="speakerBtn" onclick="chatClient.toggleSpeaker()" style="display:none;" title="免提/听筒">
         <i class="fas fa-volume-up"></i>
     </button>
     ```

**预期效果：**
- 视频通话接通后，移动端和PC端都会显示：
  - ✅ 静音/取消静音按钮
  - ✅ 开启/关闭摄像头按钮
  - ✅ 切换前后摄像头按钮（仅移动端有前置/后置摄像头的设备）
  - ✅ 免提/听筒切换按钮
  - ✅ 挂断按钮

---

### 2. 通话时长显示问题 🔍

**问题描述：**
- 挂断电话后，通话记录消息中的通话时长显示为0秒

**可能原因分析：**

#### 原因1：callStartTime 未正确设置
根据之前的修复，`callStartTime`应该在ICE连接成功时才设置：
```javascript
// chat.js 第10925-10940行
if (state === 'connected' || state === 'completed') {
    console.log('✅ ICE 连接成功，媒体通道已打通');
    if (this.callState !== 'connected') {
        this.callState = 'connected';
        
        // 🔧 关键修复：在 ICE 连接成功时才记录通话开始时间
        this.callStartTime = Date.now();
        console.log('⏱️ 通话开始时间:', new Date(this.callStartTime).toLocaleTimeString());
        
        // 🔧 新增：启动通话时长定时器
        this.startCallDurationTimer();
        
        this.updateCallUI('connected');
        console.log('✅ 通话已完全建立');
    }
}
```

#### 原因2：duration 计算错误
在`endCall`方法中：
```javascript
// chat.js 第11418-11430行
let callDuration = 0;
if (this.callStartTime) {
    callDuration = Math.floor((Date.now() - this.callStartTime) / 1000); // 转换为秒
    console.log('⏱️ 通话时长:', callDuration, '秒', '(callStartTime:', new Date(this.callStartTime).toLocaleTimeString(), ')');
} else {
    console.warn('⚠️ callStartTime 为空，通话未接通，时长将为0');
}
```

#### 原因3：后端未正确接收或保存 duration
后端日志应该显示：
```python
# consumers.py 第1288行
logger.info(f"📤 转发 call_end 信令: from={from_user_id}, room={room_id}, duration={duration}s, media_type={media_type}, reason={reason}")

# consumers.py 第1430行
logger.info(f"📞 创建通话记录: room={room_id}, from={from_user_id}, duration={duration}s, media_type={media_type}, reason={reason}")

# consumers.py 第1477行
logger.info(f"✅ 通话记录消息已创建: message_id={message.id}, room={room_id}, status={call_status}, duration={message.call_duration}s")
```

**调试步骤：**

1. **检查前端日志**
   - 打开浏览器控制台
   - 发起一次视频通话并接通
   - 观察以下日志：
     ```
     ⏱️ 通话开始时间: ...
     ⏱️ 通话时长: X 秒 (callStartTime: ...)
     📤 准备发送 call_end 信令: {"type":"call_end","room_id":...,"duration":X,...}
     ✅ 已发送 call_end 信令，时长: X 秒
     ```

2. **检查后端日志**
   - 查看Django日志
   - 观察以下日志：
     ```
     📤 转发 call_end 信令: from=..., room=..., duration=Xs, ...
     📞 创建通话记录: room=..., from=..., duration=Xs, ...
     ✅ 通话记录消息已创建: message_id=..., room=..., status=completed, duration=Xs
     ```

3. **检查数据库**
   ```sql
   SELECT id, content, message_type, call_duration, call_type, call_status 
   FROM chat_message 
   WHERE message_type IN ('call_audio', 'call_video')
   ORDER BY id DESC 
   LIMIT 10;
   ```

**如果duration仍然是0，可能的解决方案：**

#### 方案A：检查ICE连接状态
确保`iceConnectionState`确实是`connected`或`completed`：
```javascript
console.log('🔍 ICE连接状态:', this.peerConnection?.iceConnectionState);
```

#### 方案B：添加备用计时机制
如果ICE连接状态不可靠，可以在发送offer时就记录时间（但这会包含等待接听的时间）：
```javascript
// 在 initiateCall 方法中
this.callStartTime = Date.now(); // 记录发起时间
```

然后在`endCall`中判断：
```javascript
if (!this.callStartTime && this.offerSentTime) {
    // 如果ICE连接未成功，使用offer发送时间作为备用
    callDuration = Math.floor((Date.now() - this.offerSentTime) / 1000);
    console.log('⚠️ 使用备用计时（包含等待接听时间）');
}
```

---

## 测试清单

### 测试1：移动端按钮显示
- [ ] 在手机端发起视频通话
- [ ] 对方接听后，检查是否显示以下按钮：
  - [ ] 静音/取消静音
  - [ ] 开启/关闭摄像头
  - [ ] 切换前后摄像头
  - [ ] 免提/听筒
  - [ ] 挂断

### 测试2：PC端按钮显示
- [ ] 在PC端发起视频通话
- [ ] 对方接听后，检查是否显示以下按钮：
  - [ ] 静音/取消静音
  - [ ] 开启/关闭摄像头
  - [ ] 挂断
  - [ ] （不应该显示切换前后摄像头和免提按钮，或者显示但不影响使用）

### 测试3：通话时长记录
- [ ] 发起视频通话并接通
- [ ] 通话至少10秒
- [ ] 挂断电话
- [ ] 检查聊天室中的通话记录消息
- [ ] 确认显示的时长与实际通话时长一致（允许±2秒误差）

### 测试4：拒绝通话
- [ ] 用户A发起通话
- [ ] 用户B点击"拒绝"
- [ ] 检查聊天室中是否显示"已拒绝语音/视频通话"

### 测试5：超时未接听
- [ ] 用户A发起通话
- [ ] 用户B不接听，等待60秒
- [ ] 检查是否自动挂断并提示"对方未接听，已自动挂断"
- [ ] 检查聊天室中是否显示"未接听语音/视频通话"

---

## 已知限制

1. **切换前后摄像头**：仅在支持`facingMode`约束的设备上有效（大多数现代手机和平板）
2. **免提模式**：Web Audio API的`setSinkId`在某些浏览器中可能不支持
3. **通话时长精度**：由于网络延迟和ICE协商时间，实际记录的时长可能与用户感知的时长有1-2秒误差

---

## 下一步优化建议

1. **添加通话质量监控**：显示网络延迟、丢包率等信息
2. **支持屏幕共享**：在视频通话中添加屏幕共享功能
3. **多人通话**：支持群组视频通话
4. **通话录音**：添加通话录音功能（需要用户授权）
5. **离线通知**：当用户离线时，通过推送通知提醒有来电
