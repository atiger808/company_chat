# 通话消息记录功能 - 部署说明

## ✅ 已完成的工作

### 1. 数据库层 (chat/models.py)
- ✅ 在Message模型中添加了`call_audio`和`call_video`两种消息类型
- ✅ 添加了`call_duration`（通话时长）、`call_type`（通话类型）、`call_status`（通话状态）字段

### 2. 后端逻辑 (chat/consumers.py)
- ✅ 在CallConsumer中添加了`create_call_record_message`方法，用于创建通话记录消息
- ✅ 在`handle_call_end`方法中调用该方法，传入通话时长、类型和状态
- ✅ 添加了`send_global_notification_for_call`方法，用于广播通话记录

### 3. 前端逻辑 (static/js/chat.js)
- ✅ 添加了`callStartTime`和`callDurationTimer`属性用于追踪通话时长
- ✅ 在ICE连接成功时记录开始时间并启动定时器
- ✅ 创建了`startCallDurationTimer`方法，每秒更新UI显示的通话时长
- ✅ 修改了`endCall`方法，计算通话时长并发送给后端
- ✅ 添加了`renderCallRecordMessage`方法，用于渲染通话记录消息

### 4. CSS样式 (static/css/chat.css)
- ✅ 添加了`.call-record`及其子元素的样式
- ✅ 支持不同状态的样式（completed/missed/rejected/cancelled）
- ✅ 图标颜色区分语音（绿色）和视频（紫色）

## 📋 需要执行的步骤

### 步骤1：运行数据库迁移

在项目根目录打开PowerShell或CMD，执行以下命令：

```powershell
# 生成迁移文件
python manage.py makemigrations chat

# 应用迁移
python manage.py migrate
```

### 步骤2：重启Django服务器

如果Django服务器正在运行，请重启它：

```powershell
# 停止当前服务器（Ctrl+C）
# 然后重新启动
python manage.py runserver
```

### 步骤3：测试功能

1. **登录两个不同的账号**
2. **发起语音或视频通话**
3. **接通后等待几秒**
4. **挂断电话**
5. **查看聊天记录**，应该能看到类似微信的通话记录：
   - ✅ 已完成的通话显示："语音通话 05:23" 或 "视频通话 01:30"
   - ❌ 未接听的通话显示："未接听语音通话"
   - 🚫 已拒绝的通话显示："已拒绝语音通话"
   - 🔕 已取消的通话显示："已取消语音通话"

## 🎨 UI效果说明

通话记录消息将显示为：

```
┌─────────────────────────────┐
│ 📞 语音通话                 │
│    05:23                    │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 📹 视频通话                 │
│    未接听                   │
└─────────────────────────────┘
```

- **图标**：语音通话使用电话图标（绿色），视频通话使用摄像机图标（紫色）
- **时长**：已完成的通话显示通话时长（分:秒格式）
- **状态**：未接听/已拒绝/已取消的通话显示相应状态文本（灰色）

## 🔍 验证要点

1. **通话时长准确性**：确认显示的通话时长与实际通话时间一致
2. **消息同步**：确认双方都能看到通话记录
3. **历史记录**：刷新页面后，通话记录仍然保留在聊天历史中
4. **不同状态**：测试各种情况（正常挂断、未接听、拒绝、取消）
5. **消息类型**：确认语音通话和视频通话的图标和文字正确区分

## 🐛 常见问题排查

### 问题1：看不到通话记录消息
- 检查是否运行了数据库迁移
- 检查浏览器控制台是否有JavaScript错误
- 确认后端日志中是否有"通话记录消息已创建"的日志

### 问题2：通话时长显示不正确
- 检查前端是否正确记录了`callStartTime`
- 检查WebSocket信令中是否包含了`duration`字段
- 检查后端是否正确接收并保存了时长数据

### 问题3：样式显示异常
- 清除浏览器缓存（Ctrl+Shift+Delete）
- 确认CSS文件已正确加载
- 检查浏览器开发者工具中的样式是否生效

## 📝 技术实现细节

### 消息数据结构
```javascript
{
    id: 123,
    message_type: 'call_audio' | 'call_video',
    content: '语音通话 05:23',
    call_duration: 323,  // 秒
    call_type: 'audio' | 'video',
    call_status: 'completed' | 'missed' | 'rejected' | 'cancelled',
    sender: {...},
    timestamp: '2024-01-01T12:00:00Z'
}
```

### 关键代码位置
- 渲染方法：`renderCallRecordMessage()` (chat.js 约第4276行)
- CSS样式：`.call-record` (chat.css 约第751行)
- 后端创建：`create_call_record_message()` (consumers.py 约第1358行)

## ✨ 功能特性

1. **实时追踪**：通话过程中实时显示通话时长
2. **自动记录**：挂断后自动创建通话记录消息
3. **状态区分**：清晰标识通话的不同状态
4. **类型区分**：语音和视频通话使用不同的图标和颜色
5. **双向同步**：双方都能看到完整的通话记录
6. **历史保存**：通话记录永久保存在聊天历史中

---

**部署完成后，您就可以像微信一样查看和管理通话记录了！** 🎉
