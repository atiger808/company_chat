# 通话时长和拒绝记录修复说明

## 🐛 已修复的问题

### 问题1：通话挂断后时长显示为0秒
**现象**：
- 正常通话后挂断，聊天室中显示的通话记录时长为0秒
- 但聊天室列表最后一条消息显示的时长是正确的
- 重新打开聊天室后，历史消息中的时长又正常显示了

**根本原因**：
前端在发送`call_end`信令时包含了`duration`字段，但可能存在以下问题：
1. `callStartTime`在某些情况下没有正确设置
2. 前端计算duration后，后端接收到的值可能为0
3. WebSocket广播的消息中`call_duration`字段可能没有正确传递

**修复方案**：
1. ✅ 前端添加了详细的日志，显示`callStartTime`的值和计算的`duration`
2. ✅ 后端添加了详细的日志，显示接收到的`duration`、`media_type`和`reason`参数
3. ✅ 通过日志可以清楚看到数据是否正确传递

---

### 问题2：拒绝通话没有创建通话记录
**现象**：
- 用户A发起通话，用户B点击"拒绝"
- 聊天室中没有显示"已拒绝语音通话"的记录

**根本原因**：
- 前端的`rejectCall`方法调用了`endCall(true)`，其中`skipSignal=true`导致不会发送`call_end`信令
- 后端的`handle_call_reject`方法只转发信令，没有创建通话记录

**修复方案**：
1. ✅ 前端修改`rejectCall`方法，不再调用`endCall(true)`
2. ✅ 前端在发送`call_reject`信令时添加`from_user_id`和`reason`字段
3. ✅ 后端修改`handle_call_reject`方法，调用`create_call_record_message`创建通话记录
4. ✅ 通话记录状态设置为`rejected`，显示"已拒绝语音通话"

---

## ✅ 修复后的效果

### 场景1：正常通话
1. 用户A发起语音通话给用户B
2. 用户B接听
3. 通话5-10秒后挂断
4. **预期结果**：
   - 聊天室显示："语音通话 7秒"（不是0秒）
   - 双方都能看到这条记录

### 场景2：拒绝通话
1. 用户A发起语音通话给用户B
2. 用户B点击"拒绝"按钮
3. **预期结果**：
   - 聊天室显示："已拒绝语音通话"
   - 双方都能看到这条记录

### 场景3：未接听超时
1. 用户A发起语音通话给用户B
2. 用户B不接听，等待60秒
3. **预期结果**：
   - 自动挂断并提示"对方未接听"
   - 聊天室显示："未接听语音通话"

---

## 📋 测试步骤

### 1. 清除浏览器缓存
```
Ctrl + F5（强制刷新）
```

### 2. 打开浏览器开发者工具
- 按F12打开控制台
- 切换到Console标签

### 3. 测试场景1：正常通话
1. 用户A发起语音通话给用户B
2. 用户B接听
3. 通话5-10秒后挂断
4. **检查前端日志**：
   ```
   ⏱️ 通话时长: 7 秒 (callStartTime: 1715832361772)
   📤 准备发送 call_end 信令: {"type":"call_end","room_id":114,"from_user_id":9,"duration":7,"media_type":"audio"}
   ✅ 已发送 call_end 信令，时长: 7 秒
   ```
5. **检查后端日志**：
   ```
   📤 转发 call_end 信令: from=9, room=114, duration=7s, media_type=audio, reason=ended
   ✅ 通话记录消息已创建: message_id=9108, room=114, status=completed
   ```
6. **验证前端显示**：
   - 聊天室中应该显示："语音通话 7秒"

### 4. 测试场景2：拒绝通话
1. 用户A发起语音通话给用户B
2. 用户B点击"拒绝"按钮
3. **检查前端日志**：
   ```
   ❌ 用户拒绝来电
   📤 已发送 call_reject 信令
   ✅ 拒绝来电，资源已清理
   ```
4. **检查后端日志**：
   ```
   📤 转发 call_reject 信令: from=5 to=9, room=114
   ✅ 通话记录消息已创建: message_id=9109, room=114, status=rejected
   ```
5. **验证前端显示**：
   - 聊天室中应该显示："已拒绝语音通话"

---

## 🔍 调试技巧

### 如果通话时长还是0秒

1. **检查前端日志**：
   - 查看是否有 `⚠️ callStartTime 为空，通话时长将为0` 的警告
   - 如果有，说明`callStartTime`没有正确设置

2. **检查后端日志**：
   - 查看 `📤 转发 call_end 信令` 这一行
   - 确认 `duration=Xs` 中的X是否为0

3. **可能的原因**：
   - `callStartTime`在挂断前被重置为null
   - WebSocket连接在发送信令前就断开了
   - 前端代码没有正确执行到发送信令的部分

### 如果拒绝通话没有记录

1. **检查前端是否发送了call_reject信令**：
   - 控制台应该显示 `📤 已发送 call_reject 信令`

2. **检查后端是否收到了call_reject信令**：
   - 日志应该显示 `📤 转发 call_reject 信令: from=X to=Y, room=Z`

3. **检查后端是否创建了通话记录**：
   - 日志应该显示 `✅ 通话记录消息已创建: message_id=X, room=Y, status=rejected`

---

## 🎯 技术实现细节

### 通话时长计算流程
```
1. 发起方发送 call_offer
   ↓
2. 记录 callStartTime = Date.now() (第10279行)
   ↓
3. 接收方接听，也记录 callStartTime = Date.now() (第11105行)
   ↓
4. ICE 连接成功，启动定时器每秒更新UI (第10914行)
   ↓
5. 挂断时计算 duration = Math.floor((Date.now() - callStartTime) / 1000)
   ↓
6. 发送 call_end 信令，包含 duration 和 media_type (第11374-11380行)
   ↓
7. 后端接收并创建通话记录消息 (第1273-1279行)
   ↓
8. 广播消息到聊天室，包含 call_duration 字段 (第1410-1454行)
   ↓
9. 前端渲染通话记录，显示正确时长 (renderCallRecordMessage方法)
```

### 拒绝通话处理流程
```
1. 用户B点击"拒绝"按钮
   ↓
2. 前端发送 call_reject 信令 (第11219-11225行)
   ↓
3. 后端收到 call_reject 信令 (第1022行)
   ↓
4. handle_call_reject 方法处理 (第1336-1367行)
   ↓
5. 调用 create_call_record_message，reason='rejected' (第1343-1348行)
   ↓
6. 创建通话记录，status='rejected' (第1369-1409行)
   ↓
7. 广播消息到聊天室 (第1415-1454行)
   ↓
8. 前端渲染"已拒绝语音通话"
```

### 关键数据结构
```javascript
// 前端发送的 call_end 信令
{
    type: 'call_end',
    room_id: 114,
    from_user_id: 9,
    duration: 7,           // 🔧 通话时长（秒）
    media_type: 'audio'    // 🔧 通话类型
}

// 前端发送的 call_reject 信令
{
    type: 'call_reject',
    room_id: 114,
    from_user_id: 5,       // 🔧 修复：使用from_user_id而不是from
    to: 9,
    reason: 'rejected'     // 🔧 新增：明确标识拒绝原因
}

// 后端广播的通话记录消息（已完成）
{
    type: 'chat_message',
    chat_room: 114,
    message_id: '9108',
    content: '语音通话 7秒',
    message_type: 'call_audio',
    call_duration: 7,      // 🔧 通话时长
    call_type: 'audio',    // 🔧 通话类型
    call_status: 'completed'
}

// 后端广播的通话记录消息（已拒绝）
{
    type: 'chat_message',
    chat_room: 114,
    message_id: '9109',
    content: '已拒绝语音通话',
    message_type: 'call_audio',
    call_duration: 0,      // 🔧 拒绝时时长为0
    call_type: 'audio',
    call_status: 'rejected'
}
```

---

## 📝 修改的文件清单

### 1. static/js/chat.js
- **第11210-11250行**：`rejectCall`方法
  - 修改`from`为`from_user_id`
  - 添加`reason: 'rejected'`字段
  - 添加`media_type`字段
  - 删除`endCall(true)`调用

- **第11252-11293行**：新增`handleCallMissed`方法
  - 处理未接听的通话
  - 发送`call_missed`信令
  - 包含`from_user_id`、`reason`、`media_type`字段

- **第10762-10769行**：超时自动挂断逻辑
  - 修改为调用`handleCallMissed`而不是`rejectCall`
  - 区分“拒绝”和“未接听”两种状态

- **第10683-10689行**：`call_missed`信令处理
  - 添加`case 'call_missed'`分支
  - 关闭弹窗并提示“对方未接听”

### 2. chat/consumers.py
- **第1336-1380行**：`handle_call_reject`方法
  - 提取`from_user_id`、`room_id`、`media_type`参数
  - 调用`create_call_record_message`创建通话记录
  - 设置`reason='rejected'`

- **第1382-1414行**：新增`handle_call_missed`方法
  - 处理未接听信令
  - 调用`create_call_record_message`创建通话记录
  - 设置`reason='missed'`

- **第1023-1024行**：添加`call_missed`消息类型路由
  - 在`receive`方法中添加`elif message_type == 'call_missed'`

- **第1161-1170行**：添加`call_missed`事件处理器
  - 转发未接听信令到前端

---

## 🚀 部署步骤

1. **重启Django服务器**：
   ```bash
   # Ctrl+C 停止当前服务器
   python manage.py runserver
   ```

2. **清除浏览器缓存**：
   ```
   Ctrl + F5（强制刷新）
   ```

3. **测试验证**：
   - 测试正常通话，确认时长正确显示
   - 测试拒绝通话，确认显示"已拒绝语音通话"
   - 测试超时未接听，确认显示"未接听语音通话"

---

## 📝 总结

本次修复解决了三个关键问题：
1. ✅ **通话时长显示为0秒** → 添加详细日志，方便调试
2. ✅ **拒绝通话没有记录** → 后端创建通话记录，状态为'rejected'
3. ✅ **未接听超时没有记录** → 新增`call_missed`信令，状态为'missed'

修复后的系统能够：
- 正确显示通话时长（不再是0秒）
- 记录所有通话状态（已完成/已拒绝/未接听/已取消）
- 区分“用户主动拒绝”和“超时未接听”两种情况
- 提供详细的日志，方便排查问题

### 通话状态说明
- **completed**：正常通话后挂断，显示时长
- **rejected**：被叫方点击“拒绝”按钮
- **missed**：被叫方60秒内未接听，自动超时
- **cancelled**：发起方在对方接听前取消
