# 企业聊天室（企联云）项目结构文档

> 基于 Django + Channels 的企业级即时通讯与协同办公平台

---

## 目录结构概览

```
company_chat/                          # 项目根目录
├── manage.py                          # Django 管理入口
├── company_chat/                      # Django 主配置
├── accounts/                          # 用户认证模块
├── chat/                              # 即时通讯模块
├── cloud/                             # 企业网盘模块
├── oa/                                # OA办公模块（考勤+审批）
├── tasks/                             # 任务管理模块
├── utils/                             # 工具库
├── templates/                         # 前端模板
├── static/                            # 静态资源
├── media/                             # 用户上传文件
└── .env                               # 环境变量
```

---

## 一、项目配置 `company_chat/`

| 文件 | 说明 |
|------|------|
| `settings.py` | Django 主配置：数据库、中间件、INSTALLED_APPS、REST Framework、Channels、Celery |
| `urls.py` | 根路由：API路由、页面路由分发 |
| `asgi.py` | ASGI 入口（Channels WebSocket） |
| `wsgi.py` | WSGI 入口 |
| `celery_app.py` | Celery 异步任务配置 |
| `routing.py` | Channels WebSocket 路由聚合 |

### 注册的 APP

```python
INSTALLED_APPS = [
    'accounts',    # 用户认证
    'chat',        # 即时通讯
    'cloud',       # 企业网盘
    'oa',          # OA办公
    'tasks',       # 任务管理
    # ... Django 自带 app
]
```

### 根路由

| 路径 | 目标 |
|------|------|
| `/` | 首页 |
| `/api/` | DRF API 根 |
| `/api/auth/` | 账户 REST API |
| `/api/chat/` | 聊天 REST API |
| `/api/cloud/` | 网盘 REST API |
| `/api/oa/` | OA REST API |
| `/api/tasks/` | 任务 REST API |
| `/chat/` | 聊天室页面 |
| `/oa/attendance/` | 考勤打卡页面 |
| `/oa/approval/` | OA审批页面 |
| `/cloud/` | 企业网盘页面 |
| `/admin/` | Django Admin |
| `/tasks/` | 任务管理页面 |

---

## 二、用户认证模块 `accounts/`

### 核心功能
- 用户注册/登录/密码重置
- JWT 认证（`rest_framework_simplejwt`）
- 验证码/滑块验证码登录
- 用户资料管理（头像、部门、角色）
- 操作日志、登录日志记录

### 关键文件

| 文件 | 说明 |
|------|------|
| `models.py` | `CustomUser`（自定义用户模型）、`Department`（部门）、`LoginLog`、`OperationLog`、`ConsultationRequest` |
| `views.py` | 注册/登录/信息/密码重置/验证码/管理后台 API |
| `serializers.py` | 用户序列化器 |
| `urls.py` | `/api/auth/` 路由 |
| `middleware.py` | 登录验证中间件 |
| `permissions.py` | 自定义权限类 |
| `migrations/` | 数据库迁移文件 |

### 用户角色

| 角色 | 说明 |
|------|------|
| `super_admin` | 超级管理员（所有权限） |
| `admin` | 管理员 |
| `user` | 普通用户 |

---

## 三、即时通讯模块 `chat/`

### 核心功能
- 私聊/群聊消息收发
- WebSocket 实时通信（Django Channels）
- 消息类型：文本、图片、语音、视频、文件、表情、任务卡片
- 消息引用（回复）
- 消息加密传输（AES/SM4）
- 聊天室管理（创建/加入/退出/解散）
- 文件上传与同步到网盘
- 音视频通话（WebRTC）
- @提及所有人
- 未读消息计数

### 关键文件

| 文件 | 说明 |
|------|------|
| `models.py` | `ChatRoom`（聊天室）、`Message`（消息）、`MessageFile`（文件）、`ChatRoomMember` |
| `views.py` | 聊天室 CRUD、消息 CRUD、文件上传、历史消息、管理员接口 |
| `serializers.py` | 消息/文件序列化器 |
| `consumers_encrypt.py` | WebSocket 消费者（加密通信） |
| `urls.py` | `/api/chat/` 路由 |
| `routing.py` | WebSocket 路由 |
| `middleware.py` | WebSocket JWT 认证中间件 |
| `utils.py` | 工具函数 |
| `tasks.py` | Celery 任务 |
| `pagination.py` | 分页配置 |

### WebSocket 事件

| 事件类型 | 说明 |
|---------|------|
| `chat.message` | 文本消息 |
| `chat.image` | 图片消息 |
| `chat.voice` | 语音消息 |
| `chat.file` | 文件消息 |
| `chat.location` | 位置消息 |
| `chat.video` | 视频消息 |
| `chat.emoji` | 表情消息 |
| `chat.task_card` | 任务卡片 |
| `chat.quote` | 引用消息 |
| `call.*` | 音视频通话信令 |
| `typing` | 正在输入 |
| `message_read` | 消息已读 |

---

## 四、企业网盘模块 `cloud/`

### 核心功能
- 文件/文件夹 CRUD（多级目录）
- 文件上传（断点续传）
- 文件预览（图片、视频、文档）
- 文件分享（链接+密码+二维码）
- OnlyOffice 文档在线协作编辑
- 协作文档管理
- 共享文件夹（成员+权限管理）
- 回收站
- 操作日志
- 系统配置管理

### 关键文件

| 文件 | 说明 |
|------|------|
| `models.py` | `CloudFile`（文件）、`Folder`（文件夹）、`FileShare`（分享）、`CollaborativeDocument`（协作文档）、`FileOperationLog`、`FolderCollaboration`（共享文件夹协作）、`CloudSystemConfig` |
| `views.py` | 文件/文件夹 CRUD、分享、协作、配置、操作日志、统计 API |
| `serializers.py` | 序列化器 |
| `urls.py` | `/api/cloud/` 路由 |
| `permissions.py` | 文件权限控制 |
| `filters.py` | 文件搜索过滤 |
| `managers.py` | 文件管理逻辑 |
| `middleware.py` | 访问控制中间件 |
| `consumers.py` | WebSocket 消费者 |
| `websocket_utils.py` | WebSocket 工具函数 |
| `routing.py` | WebSocket 路由 |
| `share_urls.py` | 公共分享页面路由 |
| `pagination.py` | 分页配置 |

### 文件权限级别

| 权限 | 说明 |
|------|------|
| `read` | 只读 |
| `write` | 可编辑 |
| `admin` | 管理员 |

---

## 五、OA办公模块 `oa/`

### 核心功能
- **考勤打卡**：上班/下班打卡、位置记录、月度统计
- **OA审批**：审批流程（顺序/并行审批）、审批节点、审批人配置
- **工作通知**：审批/考勤/任务/协作/系统通知、WebSocket 实时推送
- **百度地图集成**：坐标转换、反向地理编码

### 关键文件

| 文件 | 说明 |
|------|------|
| `models.py` | `AttendanceRecord`（打卡记录）、`ApprovalRequest`（审批请求）、`ApprovalNode`（审批节点）、`ApprovalAssignee`（审批人）、`ApprovalLog`（审批日志）、`WorkNotification`（工作通知） |
| `views.py` | 考勤打卡 API、审批流程 API、通知 API |
| `serializers.py` | 序列化器 |
| `urls.py` | `/api/oa/` 路由 |

### 审批流程模式

| 模式 | 说明 |
|------|------|
| `parallel` | 并行审批（所有节点同时审批） |
| `sequential` | 顺序审批（节点依次审批） |

### 审批签署方式

| 方式 | 说明 |
|------|------|
| `orsign` | 或签（一人通过即可） |
| `countersign` | 会签（需全部通过） |

### 审批类型

| 类型 | 说明 |
|------|------|
| `leave` | 请假 |
| `overtime` | 加班 |
| `expense` | 报销 |
| `trip` | 出差 |
| `purchase` | 采购 |
| `other` | 其他 |

---

## 六、任务管理模块 `tasks/`

### 核心功能
- 任务 CRUD
- 任务分配
- 任务状态跟踪
- 任务卡片分享到聊天室

### 关键文件

| 文件 | 说明 |
|------|------|
| `models.py` | `Task`（任务）、`TaskAssignment`（任务分配） |
| `views.py` | 任务 API |
| `serializers.py` | 序列化器 |
| `urls.py` | `/api/tasks/` 路由 |

---

## 七、工具库 `utils/`

| 文件 | 说明 |
|------|------|
| `encrypt_aes.py` | AES 加解密（API 数据加密） |
| `reverse_geocoding_to_city.py` | 百度地图反向地理编码 + WGS84→BD09 坐标转换 |
| `request_util.py` | 请求工具（获取客户端 IP） |
| `mixins.py` | 通用混入类 |
| `utils.py` | 通用工具函数 |

---

## 八、前端模板 `templates/`

### 聊天模块

| 模板 | 说明 |
|------|------|
| `chat/chat.html` | 聊天室主页面 |
| `chat/login.html` | 登录页 |
| `chat/register.html` | 注册页 |
| `chat/admin.html` | 管理控制台 |
| `chat/reset-password.html` | 密码重置 |
| `chat/reset-password-error.html` | 密码重置失败 |
| `chat/generate-icons.html` | 图标生成工具 |

### 网盘模块

| 模板 | 说明 |
|------|------|
| `cloud/cloud.html` | 网盘主页面 |
| `cloud/cloud_login.html` | 网盘登录 |
| `cloud/cloud_settings.html` | 系统配置 |
| `cloud/cloud_editor.html` | OnlyOffice 文档编辑器 |
| `cloud/base.html` | 网盘基础模板 |
| `cloud/share.html` | 分享页面 |
| `cloud/share_detail.html` | 分享详情 |
| `cloud/share_password.html` | 分享密码验证 |
| `cloud/share_expired.html` | 分享已过期 |
| `cloud/share_not_found.html` | 分享不存在 |
| `cloud/share_not_allowed.html` | 分享无权限 |

### OA 模块

| 模板 | 说明 |
|------|------|
| `oa/attendance.html` | 考勤打卡页 |
| `oa/approval.html` | OA审批页 |

### 其他

| 模板 | 说明 |
|------|------|
| `index.html` | 官网首页 |
| `contact.html` | 联系我们 |
| `tasks/tasks.html` | 任务管理页 |
| `docs/api_docs.html` | API文档 |
| `emails/password_reset.html` | 密码重置邮件 |
| `emails/password_changed.html` | 密码变更通知 |
| `manifest.json` | PWA 清单 |

---

## 九、前端静态资源 `static/`

### 自定义 JS 文件

| 文件 | 说明 |
|------|------|
| `js/chat.js` | 聊天室主逻辑（消息渲染、发送、WebSocket、音视频通话、图片预览） |
| `js/cloud.js` | 网盘主逻辑（文件管理、分享、协作、预览） |
| `js/approval.js` | OA审批（创建、审批流、详情、打印） |
| `js/attendance.js` | 考勤打卡（打卡、统计、位置） |
| `js/notifications.js` | 工作通知（铃铛、下拉列表、WebSocket 推送） |
| `js/tasks.js` | 任务管理 |
| `js/admin.js` | 管理后台 |
| `js/admin_chat_rooms.js` | 聊天室管理（历史消息查看） |
| `js/admin_settings.js` | 系统设置管理 |
| `js/admin_statistics.js` | 数据统计管理 |
| `js/auth.js` | 用户认证 |
| `js/chat_auth.js` | 聊天室认证页面 |
| `js/cloud_auth.js` | 网盘认证页面 |
| `js/cloud_settings.js` | 系统配置页面 |
| `js/cloud_config.js` | 前端配置 |
| `js/frontend_cloud_config.js` | 前端云配置 |
| `js/cloud_editor.js` | 文档编辑器 |
| `js/collab_socket.js` | 协作 WebSocket |
| `js/encrypts.js` | 前端加密（AES/SM4） |
| `js/utils.js` | 工具函数 |
| `js/api.js` | API 封装 |
| `js/token.js` | Token 管理 |
| `js/slider-captcha.js` | 滑块验证码 |
| `js/service-worker.js` | PWA 离线缓存 |
| `js/md5-worker.js` | MD5 Web Worker |

### 自定义 CSS 文件

| 文件 | 说明 |
|------|------|
| `css/chat.css` | 聊天室样式 |
| `css/cloud.css` | 网盘样式 |
| `css/oa.css` | OA样式 |
| `css/tasks.css` | 任务样式 |
| `css/admin.css` | 管理后台样式 |
| `css/chat_auth.css` | 聊天室认证页样式 |
| `css/cloud_auth.css` | 网盘认证页样式 |
| `css/cloud_settings.css` | 系统配置页样式 |

### 第三方库 (`static/js/dist/`)

| 文件 | 说明 |
|------|------|
| `spark-md5.min.js` | MD5 计算 |
| `crypto-js.min.js` | 加密库 |
| `sm4.min.js` | 国密 SM4 |
| `qrcode.min.js` | 二维码生成 |
| `chart.min.js` | 数据图表 |

---

## 十、技术栈

| 组件 | 技术 |
|------|------|
| 后端框架 | Django 3.2+ / 4.2 |
| API | Django REST Framework (DRF) |
| 实时通信 | Django Channels 4.x + Daphne |
| 数据库 | SQLite（开发）/ PostgreSQL（生产） |
| 缓存/消息队列 | Redis |
| 异步任务 | Celery + Redis |
| 前端 | 原生 JavaScript + Font Awesome 6 |
| 文档协作 | OnlyOffice |
| 地图 | 百度地图 API |
| 加密 | AES + 国密 SM4 |
| 认证 | JWT (rest_framework_simplejwt) |

---

## 十一、部署相关

| 文件 | 说明 |
|------|------|
| `requirements.txt` | Python 依赖 |
| `build.sh` | 构建脚本 |
| `start_chat.sh` | 启动脚本 |
| `celery_company_chat.sh` | Celery 启动脚本 |
| `company_chat.service` | Systemd 服务配置 |
| `update_static_versions.py` | 静态文件版本号更新 |
| `.env` | 环境变量配置 |
| `prompt.txt` | 提示词配置 |

---

## 十二、数据库模型概览

### accounts 模块
- `CustomUser` — 自定义用户模型
- `Department` — 部门
- `LoginLog` — 登录日志
- `OperationLog` — 操作日志
- `ConsultationRequest` — 咨询请求

### chat 模块
- `ChatRoom` — 聊天室
- `ChatRoomMember` — 聊天室成员
- `Message` — 消息
- `MessageFile` — 消息文件
- `FileUpload` — 文件上传记录

### cloud 模块
- `CloudFile` — 网盘文件
- `Folder` — 文件夹
- `FileShare` — 文件分享
- `CollaborativeDocument` — 协作文档
- `DocumentChatMessage` — 文档聊天消息
- `FileOperationLog` — 文件操作日志（含共享文件夹操作：`create`/`delete`/`add_folder_member`/`remove_folder_member`/`upload`）
- `FolderCollaboration` — 文件夹协作（`permission` 权限、`is_active` 是否有效、`notify_private_chat` 是否以私聊通知成员）
- `CloudSystemConfig` — 系统配置
- `UploadSession` — 上传会话
- `UserOnlyOfficePermission` — OnlyOffice 权限

### oa 模块
- `AttendanceRecord` — 打卡记录
- `ApprovalRequest` — 审批请求
- `ApprovalNode` — 审批节点
- `ApprovalAssignee` — 审批人
- `ApprovalLog` — 审批日志
- `WorkNotification` — 工作通知

### tasks 模块
- `Task` — 任务
- `TaskAssignment` — 任务分配

---

## 十三、API 路由汇总

### 认证 `/api/auth/`
- `POST /register/` — 注册
- `POST /login/` — 登录
- `POST /token/` — JWT 获取
- `POST /token/refresh/` — JWT 刷新
- `GET /me/` — 当前用户信息
- `POST /change-password/` — 修改密码
- `POST /reset-password/` — 重置密码

### 聊天 `/api/chat/`
- `GET /rooms/` — 聊天室列表
- `POST /rooms/` — 创建聊天室
- `GET /messages/` — 消息列表
- `POST /messages/` — 发送消息
- `POST /upload/` — 上传文件

### 网盘 `/api/cloud/`
- `GET /files/` — 文件列表
- `POST /files/` — 上传文件
- `POST /folders/` — 创建文件夹
- `POST /folders/<id>/delete/` — 删除文件夹
- `POST /shares/` — 创建分享
- `GET /shares/` — 分享列表
- `GET /shared-folders/` — 共享文件夹列表
- `POST /shared-folders/` — 创建共享文件夹
- `DELETE /shared-folders/<id>/` — 删除共享文件夹
- `GET /shared-folders/<id>/members/` — 共享文件夹成员列表
- `POST /shared-folders/<id>/add_member/` — 添加成员（默认发送工作通知 + 私聊通知）
- `POST /shared-folders/<id>/update_member/` — 更新成员权限 / 私聊通知开关
- `POST /shared-folders/<id>/remove_member/` — 移除成员
- `GET /operation-logs/` — 文件操作日志（支持搜索、操作类型筛选）

### OA `/api/oa/`
- `GET /attendance/` — 打卡记录
- `POST /attendance/clock-in/` — 上班打卡
- `POST /attendance/clock-out/` — 下班打卡
- `GET /approval/` — 审批列表
- `POST /approval/` — 创建审批
- `POST /approval/<id>/approve/` — 通过
- `POST /approval/<id>/reject/` — 驳回
- `POST /approval/<id>/cancel/` — 撤销
- `GET /notifications/` — 通知列表

### 任务 `/api/tasks/`
- `GET /tasks/` — 任务列表
- `POST /tasks/` — 创建任务
- `PUT /tasks/<id>/` — 更新任务
