# 通话时长显示问题修复说明

## 🐛 问题描述

**现象**：
- 挂断电话后，聊天室中显示的通话记录时长为0秒
- 但聊天室列表中最后一条消息显示的时长是正确的
- 重新打开聊天室后，历史消息中的通话时长又正常显示了

**根本原因**：
前端在发送`call_end`信令时，可能没有正确包含`duration`和`media_type`字段，或者后端没有正确接收这些字段。

---

## 🔍 问题分析

### 日志分析
从后端日志可以看到：
```
2026-05-16 12:06:01.771 | INFO | chat.consumers:receive:1011 - 收到信令: type=call_end, from=9
2026-05-16 12:06:01.772 | INFO | chat.consumers:handle_call_end:1265 - 📤 转发 call_end 信令: from=9, room=114
2026-05-16 12:06:01.792 | INFO | chat.consumers:create_call_record_message:1406 - ✅ 通话记录消息已创建: message_id=9108, room=114, status=completed
```

**问题**：日志中没有显示`duration`的值，说明后端可能没有接收到这个字段。

### `type=unknown, target_id=None` 的含义

这是ICE candidate转发时的调试日志：
- **`type=unknown`**：表示ICE candidate的type字段为空或未知（不影响功能）
- **`target_id=None`**：表示ICE candidate消息中缺少`to`字段（目标用户ID）

**是否需要修复**：
- ❌ **不需要修复**：这些是调试级别的日志，不影响通话功能
- ✅ **已优化**：将日志级别从INFO改为DEBUG，避免刷屏

---

## ✅ 修复方案

### 修复1：后端添加详细日志

**文件**：`chat/consumers.py`

**修改位置**：`handle_call_end`方法（约第1256行）

```python
async def handle_call_end(self, data):
    """转发挂断信令"""
    room_id = data.get('room_id')
    from_user_id = data.get('from_user_id') or self.user.id
    duration = data.get('duration', 0)  # 🔧 明确提取duration
    media_type = data.get('media_type', 'audio')  # 🔧 明确提取media_type
    reason = data.get('reason', 'ended')  # 🔧 明确提取reason

    if not room_id:
        logger.error(f"❌ call_end 缺少 room_id: {data}")
        return

    # 🔧 关键修复：记录通话时长信息，用于调试
    logger.info(f"📤 转发 call_end 信令: from={from_user_id}, room={room_id}, duration={duration}s, media_type={media_type}, reason={reason}")
```

**效果**：
- 现在后端日志会显示完整的通话信息
- 可以清楚看到前端是否正确发送了duration

---

### 修复2：前端添加详细日志

**文件**：`static/js/chat.js`

**修改位置**：`endCall`方法（约第11326行）

```javascript
// 🔧 新增：计算通话时长
let callDuration = 0;
if (this.callStartTime) {
    callDuration = Math.floor((Date.now() - this.callStartTime) / 1000);
    console.log('⏱️ 通话时长:', callDuration, '秒', '(callStartTime:', this.callStartTime, ')');
} else {
    console.warn('⚠️ callStartTime 为空，通话时长将为0');
}

// 3. 🔧 关键修复：条件发送结束信令，包含通话时长信息
if (!skipSignal && this.callWs?.readyState === WebSocket.OPEN) {
    try {
        const endMessage = {
            type: 'call_end',
            room_id: this.callRoomId,
            from_user_id: this.currentUser.id,
            duration: callDuration,  // 🔧 新增：通话时长（秒）
            media_type: this.callType || 'audio'  // 🔧 新增：通话类型
        };
        console.log('📤 准备发送 call_end 信令:', JSON.stringify(endMessage));
        
        this.callWs.send(JSON.stringify(endMessage));
        console.log('✅ 已发送 call_end 信令，时长:', callDuration, '秒');
    } catch (e) {
        console.error('❌ 发送结束信令失败:', e.message);
    }
} else {
    console.warn('⚠️ 跳过发送 call_end 信令: skipSignal=', skipSignal, ', callWs状态=', this.callWs?.readyState);
}
```

**效果**：
- 控制台会显示完整的call_end信令内容
- 可以确认duration是否正确计算和发送
- 如果callStartTime为空，会有警告提示

---

### 修复3：优化ICE candidate日志

**文件**：`chat/consumers.py`

**修改位置**：`handle_ice_candidate`方法（约第1301行）

```python
async def handle_ice_candidate(self, data):
    """转发 ICE candidate"""
    target_id = data.get('to')
    if not target_id or target_id == self.user.id:
        # 🔧 优化日志：只在debug级别记录，避免刷屏
        logger.debug(f"⏭️ 忽略无效的 ICE candidate 转发: target_id={target_id}, from={self.user.id}")
        return
```

**效果**：
- 减少日志刷屏
- 保留调试信息供需要时使用

---

## 📋 测试步骤

### 1. 清除浏览器缓存
```
Ctrl + F5（强制刷新）
```

### 2. 打开浏览器开发者工具
- 按F12打开控制台
- 切换到Console标签

### 3. 发起一次通话测试
1. 用户A发起语音通话给用户B
2. 用户B接听
3. 通话5-10秒后挂断

### 4. 检查前端日志
在控制台中应该看到：
```
⏱️ 通话时长: 7 秒 (callStartTime: 1715832361772)
📤 准备发送 call_end 信令: {"type":"call_end","room_id":114,"from_user_id":9,"duration":7,"media_type":"audio"}
✅ 已发送 call_end 信令，时长: 7 秒
```

### 5. 检查后端日志
在Django日志中应该看到：
```
📤 转发 call_end 信令: from=9, room=114, duration=7s, media_type=audio, reason=ended
✅ 通话记录消息已创建: message_id=9108, room=114, status=completed
```

### 6. 验证前端显示
- 聊天室中应该显示："语音通话 7秒"（不是0秒）
- 聊天室列表最后一条消息也应该显示正确的时长

---

## 🔍 常见问题排查

### 问题1：前端日志显示 `callStartTime 为空`
**原因**：通话开始时没有正确记录开始时间

**解决方案**：
1. 检查`acceptCall`方法中是否有`this.callStartTime = Date.now()`
2. 检查`initiateCall`方法中是否有`this.callStartTime = Date.now()`

### 问题2：后端日志显示 `duration=0s`
**原因**：前端发送的call_end信令中duration字段为0

**解决方案**：
1. 检查前端是否正确计算了callDuration
2. 检查WebSocket连接是否正常
3. 查看前端控制台是否有错误日志

### 问题3：聊天室显示0秒，但列表显示正确时长
**原因**：WebSocket广播的消息中缺少call_duration字段

**解决方案**：
1. 检查`create_call_record_message`方法中是否正确设置了`call_duration`
2. 检查广播消息中是否包含`call_duration`字段
3. 检查前端`renderCallRecordMessage`方法是否正确读取了`message.call_duration`

---

## 🎯 技术实现细节

### 通话时长计算流程
```
1. 发起方/接听方记录 callStartTime = Date.now()
   ↓
2. 每秒更新UI显示（startCallDurationTimer）
   ↓
3. 挂断时计算 duration = Math.floor((Date.now() - callStartTime) / 1000)
   ↓
4. 发送 call_end 信令，包含 duration 和 media_type
   ↓
5. 后端接收并创建通话记录消息
   ↓
6. 广播消息到聊天室，包含 call_duration 字段
   ↓
7. 前端渲染通话记录，显示正确时长
```

### 关键数据结构
```javascript
// 前端发送的 call_end 信令
{
    type: 'call_end',
    room_id: 114,
    from_user_id: 9,
    duration: 7,           // 🔧 通话时长（秒）
    media_type: 'audio'    // 🔧 通话类型
}

// 后端广播的通话记录消息
{
    type: 'chat_message',
    chat_room: 114,
    message_id: '9108',
    content: '语音通话 7秒',
    message_type: 'call_audio',
    call_duration: 7,      // 🔧 通话时长
    call_type: 'audio',    // 🔧 通话类型
    call_status: 'completed'
}
```

---

## 📝 总结

本次修复主要解决了以下问题：
1. ✅ 添加了详细的日志，方便调试通话时长问题
2. ✅ 优化了ICE candidate日志，避免刷屏
3. ✅ 确保前端正确发送duration和media_type字段
4. ✅ 确保后端正确接收并保存通话时长

修复后，通过查看前端控制台和后端日志，可以快速定位通话时长相关的问题。
