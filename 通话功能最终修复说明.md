# 通话功能最终修复说明

## 🐛 已修复的问题

### 问题1：后端报错 `No handler for message type user_online_status`

**现象**：
```
Exception inside application: No handler for message type user_online_status
ValueError: No handler for message type user_online_status
```

**根本原因**：
- `CallConsumer`加入了通知组，收到了`user_online_status`事件但没有对应的处理方法
- 只有`NotificationConsumer`处理这个事件，但`CallConsumer`也会收到

**修复方案**：
✅ **添加`user_online_status`事件处理器**（[consumers.py](file:///E:/company_chat/chat/consumers.py#L1180-L1185)）：
```python
async def user_online_status(self, event):
    """🔧 新增：处理用户在线状态事件（避免报错）"""
    # CallConsumer 不需要处理在线状态，直接忽略
    # 这些消息应该由 NotificationConsumer 处理
    logger.debug(f"⏭️ CallConsumer 忽略 user_online_status 事件")
    pass
```

---

### 问题2：通话挂断后时长显示为0秒

**现象**：
- 正常通话后挂断，聊天室中显示的通话记录时长为0秒
- 重新打开聊天室后，历史消息中的时长又正常显示了

**根本原因**：
前端在`handleNewMessage`方法中构建消息对象时，没有包含通话相关字段（`call_duration`、`call_type`、`call_status`），导致新收到的通话记录消息无法正确渲染时长。

**修复方案**：
✅ **添加通话相关字段到消息对象**（[chat.js](file:///E:/company_chat/static/js/chat.js#L1423-L1427)）：
```javascript
const fullMessage = {
    // ... 其他字段
    // 🔧 关键修复：添加通话相关字段
    call_duration: data.call_duration || 0,
    call_type: data.call_type || null,
    call_status: data.call_status || 'completed',
    is_temp: false
};
```

---

### 问题3：移动端实时通话时长没有显示

**现象**：
- 接听方可以看到实时通话时长
- 发起方看不到实时通话时长

**根本原因**：
- 接听方在ICE连接成功时启动了定时器（第10934行）
- 发起方在收到answer后只更新了状态，没有启动定时器

**修复方案**：
✅ **发起方也启动定时器**（[chat.js](file:///E:/company_chat/static/js/chat.js#L11361-L11363)）：
```javascript
// 更新状态
this.callState = 'connected';

// 🔧 新增：启动通话时长定时器（callStartTime 已在发起时设置）
this.startCallDurationTimer();

this.updateCallUI('connected');
```

---

### 问题4：拒绝通话没有创建通话记录

**现象**：
- 用户A发起通话，用户B点击"拒绝"
- 聊天室中没有显示"已拒绝语音通话"的记录

**修复方案**：
✅ **前端修改**（[chat.js](file:///E:/company_chat/static/js/chat.js#L11210-L11250)）：
- 修改`from`为`from_user_id`
- 添加`reason: 'rejected'`和`media_type`字段
- 删除`endCall(true)`调用

✅ **后端修改**（[consumers.py](file:///E:/company_chat/chat/consumers.py#L1348-L1380)）：
- 提取`from_user_id`、`room_id`、`media_type`参数
- 调用`create_call_record_message`创建通话记录
- 设置`reason='rejected'`

---

### 问题5：未接听超时没有正确显示

**现象**：
- 60秒超时后，应该显示"未接听语音通话"
- 但实际上可能显示"已拒绝语音通话"或没有记录

**修复方案**：
✅ **新增`handleCallMissed`方法**（[chat.js](file:///E:/company_chat/static/js/chat.js#L11252-L11293)）：
- 发送`call_missed`信令
- 包含`reason: 'missed'`字段

✅ **新增`handle_call_missed`方法**（[consumers.py](file:///E:/company_chat/chat/consumers.py#L1382-L1414)）：
- 创建通话记录，状态为`missed`
- 显示"未接听语音通话"

✅ **修改超时逻辑**（[chat.js](file:///E:/company_chat/static/js/chat.js#L10773-L10780)）：
- 超时时调用`handleCallMissed`而不是`rejectCall`
- 区分"用户主动拒绝"和"超时未接听"

---

## 📋 修改的文件清单

### 1. [chat/consumers.py](file:///E:/company_chat/chat/consumers.py)

#### 修改1：添加`user_online_status`事件处理器（第1180-1185行）
```python
async def user_online_status(self, event):
    """🔧 新增：处理用户在线状态事件（避免报错）"""
    logger.debug(f"⏭️ CallConsumer 忽略 user_online_status 事件")
    pass
```

#### 修改2：`handle_call_reject`方法（第1348-1380行）
- 提取`from_user_id`、`room_id`、`media_type`参数
- 调用`create_call_record_message`创建通话记录
- 设置`reason='rejected'`

#### 修改3：新增`handle_call_missed`方法（第1382-1414行）
- 处理未接听信令
- 创建通话记录，状态为`missed`
- 设置`reason='missed'`

#### 修改4：添加`call_missed`消息类型路由（第1023-1024行）
```python
elif message_type == 'call_missed':
    await self.handle_call_missed(data)
```

#### 修改5：添加`call_missed`事件处理器（第1163-1171行）
```python
async def call_missed(self, event):
    """处理未接听信令（转发给前端）"""
    await self.send(text_data=json.dumps({
        'type': 'call_missed',
        'from_user_id': event.get('from_user_id'),
    }))
```

---

### 2. [static/js/chat.js](file:///E:/company_chat/static/js/chat.js)

#### 修改1：`handleNewMessage`方法（第1423-1427行）
```javascript
// 🔧 关键修复：添加通话相关字段
call_duration: data.call_duration || 0,
call_type: data.call_type || null,
call_status: data.call_status || 'completed',
```

#### 修改2：`handleCallAnswer`方法（第11361-11363行）
```javascript
// 🔧 新增：启动通话时长定时器（callStartTime 已在发起时设置）
this.startCallDurationTimer();
```

#### 修改3：`rejectCall`方法（第11210-11250行）
- 修改`from`为`from_user_id`
- 添加`reason: 'rejected'`和`media_type`字段
- 删除`endCall(true)`调用

#### 修改4：新增`handleCallMissed`方法（第11252-11293行）
- 处理未接听的通话
- 发送`call_missed`信令

#### 修改5：超时自动挂断逻辑（第10773-10780行）
- 修改为调用`handleCallMissed`而不是`rejectCall`

#### 修改6：`call_missed`信令处理（第10687-10693行）
```javascript
case 'call_missed':
    console.log('⏱️ 通话未接听');
    this.closeIncomingCallModal();
    this.endCall(true);
    this.showToast('对方未接听', 'info');
    break;
```

---

## ✅ 修复后的效果

| 场景 | 聊天室显示 | 说明 |
|------|-----------|------|
| 正常通话 | "语音通话 7秒" | 显示实际通话时长 |
| 拒绝通话 | "已拒绝语音通话" | 被叫方点击"拒绝"按钮 |
| 未接听超时 | "未接听语音通话" | 60秒内未接听，自动超时 |
| 视频通话 | "视频通话 7秒" | 区分音频和视频 |
| 发起方实时时长 | "00:07" | 发起方也能看到实时时长 |
| 接听方实时时长 | "00:07" | 接听方能看到实时时长 |

所有通话记录对双方都可见，并且可以通过日志进行调试！

---

## 🚀 测试步骤

1. **重启Django服务器**：
   ```bash
   python manage.py runserver
   ```

2. **清除浏览器缓存**：
   ```
   Ctrl + F5（强制刷新）
   ```

3. **测试场景**：
   - 场景1：正常通话（验证时长正确显示）
   - 场景2：拒绝通话（验证显示"已拒绝语音通话"）
   - 场景3：未接听超时（验证显示"未接听语音通话"）
   - 场景4：发起方实时时长（验证发起方能看到实时时长）
   - 场景5：检查后端日志（验证不再报`user_online_status`错误）

---

## 📝 技术细节

### 通话时长追踪流程
```
1. 发起方发送 call_offer
   ↓
2. 记录 callStartTime = Date.now() (第10279行)
   ↓
3. 接收方接听，也记录 callStartTime = Date.now() (第11105行)
   ↓
4. ICE 连接成功，启动定时器每秒更新UI
   - 接听方：第10934行
   - 发起方：第11363行（新增）
   ↓
5. 挂断时计算 duration = Math.floor((Date.now() - callStartTime) / 1000)
   ↓
6. 发送 call_end 信令，包含 duration 和 media_type
   ↓
7. 后端接收并创建通话记录消息
   ↓
8. 广播消息到聊天室，包含 call_duration 字段
   ↓
9. 前端接收到新消息，构建完整消息对象（包含call_duration等字段）
   ↓
10. 前端渲染通话记录，显示正确时长
```

### 拒绝/未接听处理流程
```
拒绝：
1. 用户B点击"拒绝"按钮
   ↓
2. 前端发送 call_reject 信令（包含from_user_id, reason, media_type）
   ↓
3. 后端收到 call_reject 信令
   ↓
4. handle_call_reject 方法处理
   ↓
5. 调用 create_call_record_message，reason='rejected'
   ↓
6. 创建通话记录，status='rejected'
   ↓
7. 广播消息到聊天室
   ↓
8. 前端渲染"已拒绝语音通话"

未接听：
1. 用户B不接听，等待60秒
   ↓
2. 超时定时器触发，调用 handleCallMissed
   ↓
3. 前端发送 call_missed 信令
   ↓
4. 后端收到 call_missed 信令
   ↓
5. handle_call_missed 方法处理
   ↓
6. 调用 create_call_record_message，reason='missed'
   ↓
7. 创建通话记录，status='missed'
   ↓
8. 广播消息到聊天室
   ↓
9. 前端渲染"未接听语音通话"
```

---

## 🎯 关键修复点总结

1. ✅ **后端报错修复**：添加`user_online_status`事件处理器
2. ✅ **通话时长显示**：前端接收新消息时包含通话相关字段
3. ✅ **移动端实时时长**：发起方和接听方都启动定时器
4. ✅ **拒绝通话记录**：后端创建通话记录，状态为'rejected'
5. ✅ **未接听超时记录**：新增`call_missed`信令，状态为'missed'

所有问题都已修复，系统现在能够：
- 正确显示通话时长（不再是0秒）
- 记录所有通话状态（已完成/已拒绝/未接听/已取消）
- 区分"用户主动拒绝"和"超时未接听"两种情况
- 发起方和接听方都能看到实时通话时长
- 不再报`user_online_status`错误
- 提供详细的日志，方便排查问题
